/** Owns generated framework policy behavior for the portable clean-project generation boundary. */
import { generatedPolicyProjectionLines } from "../../../../scripts/framework/policy-projection.mjs";

export const generatedFrameworkAgentPolicy = Object.freeze(
  generatedPolicyProjectionLines("agents"),
);

export const generatedFrameworkReadmePolicy = Object.freeze([
  "## First Prompt: Define The Project",
  "",
  "The generator invites a detailed project description and stores it only as an intake draft.",
  "At first start, Codex explains the interview, evaluates any existing draft or filled manifest,",
  "shows material strengths, gaps, and contradictions, and asks whether to refine it further or—if",
  "the definition is decision-ready—begin from the confirmed scope. The manifest can be refined as",
  "the product evolves; Codex actively helps structure and challenge it rather than expecting a",
  "finished specification from the user.",
  "Resume the intake whenever a material product, scope, contract, trust, or delivery decision changes.",
  "",
  "## Framework Capability Map",
  "",
  "These concise projections keep every installed capability visible; `instructions.md` owns the complete versioned policy and rationale.",
  "",
  ...generatedPolicyProjectionLines("readme"),
  "",
  "## Documentation Context Economy",
  "",
  "`AGENTS.md` remains the bounded always-loaded bootstrap; complete policy stays visible in `instructions.md`, and skills load progressively when relevant.",
]);

export const generatedFrameworkInstructionsPolicy = Object.freeze([
  "## Startup Repository Reconstruction",
  "",
  "At every primary startup or resume, before a new slice or pending-manifest intake, reconstruct",
  "the complete current repository. Read bootstrap/instructions/README, `docs/project.md`, and optional",
  "bounded `docs/project-context.md`; inventory active Product Roots, domains/modules/surfaces, public",
  "contracts, data/migrations, configuration and delivery, dependencies/toolchains, tests/verifiers,",
  "documentation, and composition paths. Inspect Git status/diff, branch/worktree, available upstream,",
  "untracked files, and safe repository-owned evidence/runtime markers. Use `rg` for exact anchors and",
  "`$context-retrieval` or semantic search for ownership and cross-file relationships, then read every",
  "matched source used. This is complete inventory and relationship analysis, not blind loading of",
  "every file into model context.",
  "",
  "Determine whether an authorized prior outcome/goal/slice is unfinished and whether partial,",
  "duplicated, contradictory, obsolete, generated, migration, transaction, or concurrent state needs",
  "attention. Invoke `$resume-project` for unfinished work and `$system-coherence` for material drift.",
  "Consolidate or remove only unambiguous in-scope residue, rerun affected evidence and the",
  "whole-repository course check, and only then begin new work. Preserve ambiguous, unrelated,",
  "user-owned, or concurrent changes and clarify/coordinate instead of deleting or overwriting them.",
  "A quiet Git view and remembered context never prove that no prior work exists.",
  "A trusted SessionStart inspects safe metadata only for a recent repository-bound sealed handover",
  "under ignored `tmp/codexrig-handovers/`. Before reading its prompt body or doing other work, ask",
  "whether the developer wants to resume from that exact path. After explicit acceptance, invoke",
  "`$resume-project` and validate the untrusted candidate against current repository and ownership truth.",
  "",
  "## First-Prompt Project Definition Intake",
  "",
  "While `docs/project.md` has a pending product definition, the first interaction is a definition",
  "intake rather than an implementation prompt. Begin the first response by explaining that the",
  "project and durable manifest must be understood first. Do not infer product, stack, users, data",
  "policy, provider, delivery shape, or trust boundaries from the framework or a vague request.",
  "Explain immediately that Codex will proactively help turn the user's natural-language product",
  "description into a durable manifest through successive focused questions, and that the manifest",
  "can be refined later as understanding changes. Read the entire current manifest before asking.",
  "When a creation brief or filled definition exists, evaluate it first: summarize usable confirmed",
  "content, gaps, contradictions, assumptions, and recommendations. Then ask whether the user wants",
  "to refine it further or, only when it is decision-ready, begin from the confirmed scope. If it is",
  "empty or materially incomplete, explain that choosing to get started begins the focused interview",
  "or safe disjoint work—not implementation that depends on unresolved decisions.",
  "",
  "Interview in small, decision-focused batches, challenge vague terms, contradictions, implicit",
  "scope, and premature solution choices, and follow each answer with the next material questions.",
  "Cover relevant users/problems/outcomes/workflows/success evidence; scope/non-goals/priorities and",
  "compatibility; domain language/capabilities/invariants/candidate modules; data ownership/lifecycle",
  "and migrations; external integrations and failure behavior; identity/authorization/privacy/abuse",
  "boundaries; runtime/delivery/availability/performance/observability/support constraints; and",
  "developer/orchestrator ownership plus the shared pre-slice coordination channel when independent",
  "sessions or accounts may work concurrently. Do not stop while an unknown could materially change",
  "architecture, scope, safety, acceptance, or delivery.",
  "",
  "Regularly restate confirmed facts, assumptions, and open decisions. Complete the intake only when",
  "the agent can precisely explain the product, system shape, candidate capability/module topology,",
  "constraints,",
  "non-goals, risks, and acceptance evidence and the user has had a final opportunity to correct it.",
  "Then replace pending manifest entries with concise user-confirmed durable truth and proceed to the",
  "decision-ready plan and autonomous delivery. Ask no ceremonial question and do not repeat resolved",
  "questions.",
  "",
  "Resume the same focused intake later whenever a request, discovery, or concurrent change creates",
  "material ambiguity, contradiction, or a possible change to intent, scope, acceptance, module/public",
  "contracts, data, migrations, integrations, trust, compatibility, or operations. Pause only the",
  "affected write surface, continue safe disjoint work, question until the changed outcome is",
  "precise, present the revised synthesis for correction, update durable truth, and then resume",
  "autonomously. Clarification is necessary when its answer could change the result or make a write",
  "unsafe—not merely because more conversation is possible.",
  "",
  "## Memory Isolation And Durable Truth",
  "",
  "Local Codex memory isolation is repository-local and root-bound. Memories are enabled only inside",
  "this project's clean, isolated `.codex/runtime/` `CODEX_HOME`. Generation transfers no source or sibling",
  "runtime, memory database, summary, recent",
  "input, or supporting evidence. This project's later memories may work normally, but they never",
  "flow back into the reusable framework or another project. Current repository-owned truth still",
  "outranks memory.",
  "",
  "## Research And Publication Sources",
  "",
  "When research, papers, standards, guidance, or other publications inform the work, search for and",
  "prioritize the newest relevant primary or official sources. Verify publication/update date,",
  "version, correction/retraction state, and applicability; distinguish evidence from inference and",
  "label preliminary evidence. Recency does not override source quality, but no claim may be presented",
  "as current or latest without a current search.",
  "Use older sources primarily for comparison or historical context. Treat an older foundational,",
  "controlling, or uniquely relevant source as current authority only with an explicit reason and",
  "verification that newer work has not superseded, corrected, or retracted it.",
  "",
  "## Best-Available Engineering, Not Quick Fixes",
  "",
  "Never implement a material change as a quick fix, symptom patch, local workaround, or merely the",
  "first plausible solution. Before writing, reconstruct the relevant system, identify the root",
  "cause and canonical owner, trace affected contracts and consumers, compare viable designs in",
  "whole-project context, and research current primary, official, or peer-reviewed evidence whenever",
  "it could materially change the choice. Speed, token pressure, a failing check, or an urgent Dev",
  "feedback loop never lowers this standard.",
  "Choose the best-supported durable solution within authorized scope and explicitly reject material",
  "shortcuts when the tradeoff is not obvious. A temporary mitigation requires explicit user",
  "authorization or immediate safety containment, a bounded risk and removal condition, and a durable",
  "follow-up in current work state; it is never completion. Keep Dev fast through a sound early",
  "artifact and isolated parallel or deferred verification, not through structural debt or weakened",
  "contracts.",
  "",
  "## Modular Architecture, Parallel Ownership, And Integration",
  "",
  "For non-trivial product code, start with a modular monolith and ecosystem-native boundaries.",
  "Use strategic DDD to discover cohesive bounded contexts when the business domain is complex; do",
  "not impose tactical DDD patterns or independently deployed services without product and",
  "operational evidence. Product Roots are physical discovery boundaries, not domain modules.",
  "",
  "`docs/project.md` is a current-state inventory, never a roadmap. Record only modules that exist,",
  "are integrated, and expose their real current root and contract. Put every unimplemented module",
  "idea only in the initialized `docs/future-modules.md`. The same change that implements a module",
  "adds its active manifest entry—stable name/root and responsibility, actual runtime/technology,",
  "public contract, private internals, owned state/data/migrations, tenant isolation and explicit global/control-plane",
  "exceptions, allowed dependencies, focused verifier, and steward—and",
  "removes its deferred entry. Workflow policy, plans, and speculative topology never enter the",
  "manifest.",
  "",
  "Treat every newly voiced idea, wish, possibility, question, or exploratory suggestion as an",
  "intent boundary. Implement it only when the developer clearly asks to build, change, or activate",
  "it now. A clearly deferred wish becomes a future candidate. If current-versus-future intent is",
  "materially ambiguous, ask one focused clarifying question before dependent planning or writes;",
  "until answered, classify it as non-authorizing future intent, leave `docs/project.md` unchanged,",
  "and do not implement it. Record it in `docs/future-modules.md` only after future intent is explicit",
  "or confirmed. YOLO autonomy never converts an ambiguous idea into implementation authority.",
  "",
  "### Feature-To-Domain Placement",
  "",
  "Before every authorized feature, automatically decide its architectural owner from implemented",
  "domain language and invariants, owned data and lifecycle, public contracts, actors and trust or",
  "operational boundaries, change reason, and dependencies. Choose exactly one path: extend an",
  "existing module when those forces remain cohesive; create a new module in an existing domain",
  "when it needs an independently improvable responsibility, contract, data/lifecycle owner, or",
  "dependency boundary; or establish a new domain and its first module when its language,",
  "invariants, actors, data/trust ownership, or change reason is materially distinct.",
  "Do not infer ownership from a route, screen, endpoint, requested filename, or nearest directory.",
  "Every non-trivial behavior has exactly one active module owner. UI/web, Identity and Access,",
  "public API, infrastructure/delivery, and composition remain separately owned adapting surfaces.",
  "Reject an unclassified Product Root file or generic `app`, `service`, `shared`, `common`, `utils`,",
  "`platform`, or `core` fallback. If current implementation and confirmed durable truth cannot",
  "resolve a consequential placement, ask one focused question and invoke `$architecture-evolution`",
  "before dependent writes rather than guessing. Re-run the decision when later evidence makes an",
  "existing boundary incohesive.",
  "",
  "### Product Surface Selection",
  "",
  "As soon as intake facts establish users, environment, and critical workflows, derive whether the",
  "product is web/PWA, installed mobile, installed desktop, CLI/TUI, API/service, worker,",
  "library/SDK, embedded/realtime, or a justified combination. Do not infer web from the harness.",
  "When unclear, promptly ask in outcome language where/how users work; how they move between",
  "devices; and whether install/offline/app-store distribution, OS/hardware integration, files,",
  "notifications/background work, public URLs/sharing, browser reach, or controlled updates matter.",
  "The user need not know the platform name. Give one strong evidence-backed surface recommendation",
  "with its limitations/cost and at most one close alternative, then explicitly ask the user to",
  "confirm, override, or delegate it. Record selected-but-not-integrated topology separately from",
  "real interface roots and deployments in manifest System Shape. Technology selection follows this",
  "checkpoint; later surface changes invoke `$architecture-evolution` and reshape files and delivery.",
  "",
  "### Requirement-Driven Technology Selection",
  "",
  "The root Node.js/pnpm/mise stack is Codex harness tooling, never product-stack evidence or a",
  "TypeScript/Node default. After confirmed manifest requirements and domain placement, compare each",
  "module/component's platform/ecosystem, hard/soft real-time deadline and tail jitter, throughput",
  "and resource limits, safety/FFI/hardware, data/trust/tenancy, team and debug/profile tooling,",
  "deployment/rollback/observability, support horizon, and maintenance cost. Choose the least complex",
  "language/framework/runtime that satisfies the evidence, not the most familiar recent stack.",
  "For real-time, low-jitter, systems, embedded, native-integration, or resource-bounded work,",
  "explicitly evaluate Rust and—when ABI/hardware/certification/native ecosystem or measured",
  "determinism justifies its memory-safety burden—C/C++. Benchmark consequential claims; neither is",
  "an automatic answer. Polyglot components need a stable domain/trust/native/performance/deployment",
  "boundary whose benefit exceeds extra toolchain and operator cost, plus narrow versioned",
  "contracts and assembled evidence.",
  "As soon as intake facts support a meaningful comparison, consult current primary/official",
  "ecosystem evidence, give one strong primary recommendation per distinct runtime component,",
  "explain its manifest-specific fit, cost, risk, and at most one close alternative, and explicitly",
  "ask the user to confirm, override, or delegate the choice before implementation. Silence, YOLO,",
  "or a broad implementation request is not stack confirmation. Record a confirmed pre-integration",
  "decision without activating a module; the implementation change records actual `Runtime and",
  "technology` truth with real source and toolchain evidence. `pnpm stack:detect` must ignore",
  "harness-only evidence, and completed-goal housekeeping rejects later manifest/toolchain drift.",
  "",
  "### Physical Surface Boundaries",
  "",
  "Give every web/PWA, installed mobile, installed desktop, CLI/TUI, API/service, worker,",
  "library/SDK, embedded, or real-time surface its own explicit interface directory or declared",
  "product package, entry/composition boundary, platform adapters, and exposed public client/contract.",
  "Surface implementations never share a generic `app` root or deep-import one another. A",
  "cross-platform framework may share genuinely common domain/application contracts, view models,",
  "design primitives, and tokens only through explicit separately owned shared roots; platform",
  "lifecycle, navigation, routes/screens, OS/browser adapters, assets, and delivery entrypoints stay",
  "inside their respective surface root.",
  "Keep domain/application modules, UI/presentation and web interfaces, Identity and Access, public",
  "API contracts and transport adapters, and infrastructure/deployment configuration in explicit,",
  "separately owned directories. Names",
  "follow the detected ecosystem, but the boundary is mandatory: domain code does not import web,",
  "transport, provider, or provisioning implementation; a requested web app uses a dedicated declared",
  "package or interface root; versioned public API schemas/contracts are separate from handler/adapter",
  "implementation; and IaC, CI/CD, environment wiring, and deployment manifests remain outside product",
  "runtime modules. Composition roots connect these surfaces without absorbing them. Every file has one",
  "primary owner; reject mixed `app`, `platform`, `server`, `shared`, or `config` dumping grounds.",
  "Every UI surface also separates views/screens and visual components, interaction or presentation",
  "state, navigation, transport/API clients, and domain behavior. Views consume stable application",
  "contracts or view models; visual components own no persistence, remote calls, business invariants,",
  "or deployment behavior, and a design system owns visual primitives and tokens only.",
  "Identity and Access is a dedicated trust and domain boundary, not UI state, API middleware, a",
  "generic security helper, or provider configuration. Within its explicit root, separate",
  "authentication and credentials/authenticators,",
  "authorization and policy decisions, principal/account lifecycle and user management,",
  "sessions/tokens, audit/persistence, and provider adapters behind narrow public ports. UI/web owns",
  "presentation only, API transports invoke public guards, infrastructure owns provider provisioning",
  "only, and product domains keep resource-specific invariants; none deep-imports provider,",
  "credential, grant, policy, or session internals. Authorization is server-side, deny-by-default,",
  "and reevaluated for every protected action and resource through a deny-by-default server-side policy.",
  "Provider selection, issuer/audience,",
  "redirect origins, and public client IDs use one typed Identity and Access configuration owner",
  "with environment overlays; provider credentials/signing material stay secret, and provider",
  "branding is never a product-brand fallback.",
  "A new or materially changed UI/web, Identity and Access, public API, or infrastructure surface",
  "invokes `$architecture-evolution`, physical file placement review, boundary verification, and an",
  "assembled-system check. Material Auth changes also invoke `$security-review`. Record only roots",
  "and deployed surfaces that actually exist in the manifest. `pnpm auth:check` is the portable",
  "structural guard; the active Auth module owns stack-native integration and security evidence.",
  "`scripts/verify/path-hygiene.mjs` rejects Product Root-level surface files, mixed UI concerns,",
  "platform-SDK leakage into domain/shared code, and imports between separately owned surfaces.",
  "",
  "### Source And Declaration Headers",
  "",
  "Every hand-authored textual file starts with a concise format-native description of its current",
  "purpose and owning module, surface, or operational boundary. Preserve shebang, license, and",
  "language/framework directive order; use specific responsibility text rather than syntax restatement,",
  "history, or placeholders. Classes, interfaces, protocols, records, structs, enums, and non-trivial",
  "public types receive declaration-adjacent language-native documentation of responsibility, public",
  "contract or invariants, and important boundaries.",
  "Markdown uses one descriptive top-level heading and unique stable referenceable subheadings. JSON,",
  "YAML, TOML, schemas, lockfiles, and other comment-free or schema-governed formats use only title,",
  "description, kind, name, owner, or `$schema` metadata permitted by their owner; never invent keys.",
  "Generated, vendored, lock, binary, and schema-governed artifacts remain generator/schema-owned.",
  "Update descriptions in the same change as a rename or responsibility, owner, contract, or trust",
  "boundary change. Completed-slice review checks touched descriptions; `scripts/verify/patterns.mjs`,",
  "`scripts/verify/docs.mjs`, and completed-goal plus scheduled housekeeping enforce the project rule.",
  "",
  "### Multi-Device Experience",
  "",
  "Assume every product UI serves mobile, tablet, and desktop unless the user confirms narrower",
  "durable scope. Web uses one content-driven responsive information architecture across narrow,",
  "medium, and wide viewports with fluid/container layout, wrapping/reflow, and feature parity.",
  "Support touch, pointer, keyboard, and assistive input; zoom/text reflow, orientation, safe-area",
  "and dynamic viewport/virtual keyboard behavior; responsive media; localization; and constrained",
  "CPU/network/memory. Reject user-agent or physical device-width branches, fixed desktop page",
  "widths or 100vh shells, root overflow masking, hover-only tasks, and copied device-specific",
  "domain flows. Keep responsive tokens and primitives inside presentation ownership.",
  "The UI module's broad experience verifier covers representative narrow/medium/wide viewports,",
  "input, zoom, orientation, loading/error, and slow-network states. In Dev it runs isolated beside",
  "or after the newest deploy. Surface-quality plus completed-goal and scheduled housekeeping own",
  "the portable responsive guard; material device-support changes invoke `$architecture-evolution`.",
  "",
  "### Localization And Language Strategy",
  "",
  "Source code, identifiers, filenames, tests, and technical file/declaration headers are always",
  "English. During creation and the first definition intake, before product surface and technology",
  "decisions harden, ask whether user-facing UI, public content and metadata, emails/notifications,",
  "support, and legal text need one locale or multiple locales. When relevant, confirm default,",
  "supported, and fallback locales, locale-sensitive dates/numbers/currency/plurals/direction, URL",
  "and search behavior, content/legal ownership, translation workflow, and fallback/missing-copy",
  "behavior. User-facing copy follows its configured native language; do not translate identifiers",
  "or duplicate domain logic by locale.",
  "Project-owned `config/localization.json` begins pending and is resolved before product",
  "implementation; manifest System Shape records the current decision. Each surface owns localized",
  "resources behind a narrow localization contract, shared domain values remain locale-neutral, and",
  "a user-facing language change invokes `$native-language-content-review`. Run",
  "`pnpm localization:check` directly and through setup, completed-goal, and scheduled housekeeping.",
  "",
  "### Tenant Isolation Boundary",
  "",
  "Every generated project is tenant-capable from creation through visible project-owned",
  "`config/tenancy.json`. Pending context resolution is truthful only while no product runtime",
  "exists. Before the first product slice, choose verified tenant-context sources and implement a",
  "dedicated tenancy boundary with separate context/resolution, deny-by-default policy/isolation,",
  "and narrow public-contract/port concerns. Authentication or token presence is not isolation.",
  "Never trust a tenant ID merely because it came from a header, URL/query/body, UI selector,",
  "unsigned message, or network. Authorize principal, verified tenant membership, action, and",
  "resource together, and keep tenant switching re-resolved and reauthorized. Propagate immutable",
  "request/job-scoped tenant context through commands, queries, repositories and uniqueness,",
  "migrations, cache/files/search, events/messages/jobs, quotas, observability, integrations,",
  "onboarding/offboarding, export/deletion, and backup/restore. Global/control-plane state and",
  "cross-tenant operations require an explicit separate owner, capability, least privilege, and",
  "audit; there is no implicit default tenant. Infrastructure/database isolation is defense in",
  "depth, not a replacement for application authorization.",
  "Every active module records its current tenant isolation and owns negative evidence that tenant",
  "A cannot observe or affect tenant B, including asynchronous and cache/file paths. Material",
  "tenancy changes invoke `$architecture-evolution` and `$security-review`. `pnpm tenancy:check`",
  "runs directly, at completed-goal housekeeping, and in scheduled read-only housekeeping.",
  "",
  "Consumers depend only on a module's narrow public contract. Hide framework, provider, storage,",
  "transport, and algorithm choices behind ports and adapters; translate foreign models at the edge.",
  "Reject dependency cycles, deep imports, cross-module writes to owned data, shared mutable state,",
  "and generic `utils`, `common`, or `shared` dumping grounds. Apply a replacement test: another",
  "implementation or adapter should require changes only in composition/configuration, the",
  "replacement, and an explicit data migration. Evolve contracts compatibly; use an explicit",
  "expand/migrate/contract sequence or another rollback-safe path for breaking changes.",
  "Treat modules as system components, not isolated mini-products. Keep each independently",
  "improvable or replaceable while its contracts, data semantics, versions, lifecycle, error/timeout",
  "behavior, and operational expectations remain compatible with current consumers. Verify both the",
  "focused component boundary and a realistic assembled flow so the assembled system is verified as",
  "one functioning unit.",
  "When product purpose, domain language, system shape, module ownership, or a major dependency",
  "changes materially, invoke `$architecture-evolution` before extending the old layout. Reassess",
  "keep/split/merge/rename/retire/replace decisions, dependency direction, public contracts, data",
  "ownership, and physical file placement; migrate stale files and directories with the architecture.",
  "",
  "Use one central `main` as the only durable integration branch and prohibit long-lived module,",
  "developer, and environment branches. A serialized single writer may work directly on `main`",
  "only when branch policy permits.",
  "Use this pre-slice coordination check immediately before every slice begins and before any",
  "expanded write scope: restate its goal,",
  "outcome and success condition; declare every module, public contract, schema, migration, shared",
  "configuration surface, and repository-relative file prefix plus exactly one write owner. Inspect",
  "all",
  "observable live-agent assignments, sessions, account-level work, bounded context, and shared team",
  "or orchestration channels before relying on Git. Only confirmed-disjoint slices may write in",
  "parallel; resolve overlap or uncertain shared ownership by rescoping, ordering, or one writer",
  "before implementation. A local runtime lease or quiet worktree cannot prove that another clone,",
  "machine, or account is idle.",
  "Different",
  "developers/accounts use temporary task branches in separate clones with separate OS or provider",
  "credential contexts. Same-account tasks may use Git worktrees; a worktree is workspace isolation,",
  "not an authentication boundary, and may share Git configuration or credential helpers. Every",
  "Codex session uses its own clone/worktree `.codex/runtime/` home; credentials stay outside Git. Read-heavy",
  "discovery may run broadly in parallel; write-heavy work requires disjoint scopes.",
  "Shared-contract work has one integrator, lands compatibility first, and migrates consumers in",
  "dependency order. Integrate small batches to `main` frequently; a goal is not complete on a task",
  "branch. One integrator or the detected provider's merge serializer publishes it, then the actual",
  "resulting `main` receives the course check, affected review, completed-goal documentation review,",
  "any critical-document confirmation and preservation review, fresh audit, and verification before",
  "`pnpm goal:new`. Keep both provider CI adapters portable; apply provider-specific protection,",
  "metadata, and merge serialization only after remote and ownership identities are known.",
  "",
  "After every completed non-trivial implementation, architecture, configuration-boundary, or",
  "integration slice, invoke `$system-coherence` before acceptance. Inspect the change through real",
  "consumers and a representative assembled flow, search for competing sources of truth and semantic",
  "duplication, and repair material ownership, contract, dependency, data, configuration, layout, or",
  "integration drift at the canonical owner. In Dev this runs in parallel with or after the latest",
  "developer deploy, but it finishes before the slice becomes stable or promotable.",
  "Then finish focused evidence, review/repair to zero relevant findings, and the fresh audit before",
  "performing a whole-repository course check; repeat it at every major",
  "milestone and completed goal. Refresh available upstream",
  "state and compare concurrent changes by path, module, public contract, schema, and migration.",
  "Disjoint changes continue without interruption. Overlap stops further writes until the change is",
  "integrated or ownership and order are reconciled; rerun only affected evidence. Remote uncertainty",
  "blocks shared-boundary work but need not stop isolated disjoint work.",
  "",
  "## Subagent Orchestration And Integration Authority",
  "",
  "The primary owns the plan, delegation graph, policy files, shared contracts, integration, and every",
  "commit, merge, push, publication, or deployment. Use subagents only for at least two substantial",
  "independent slices. Unless the user explicitly sets another ceiling, admit at most four live",
  "subagents in one wave and prohibit nested delegation. Before every spawn, inspect available goal",
  "and completion budget, absolute or percentage remaining capacity, active rate windows, confirmed",
  "additive or redeem/reset credits, and the reserve needed for integration, review, repair, verification,",
  "and final reporting. Respect every signal's declared unit and scope; never assume a daily, weekly,",
  "renewable, windowed, or percentage-based quota. Do not",
  "start work that cannot finish with a structured handoff inside that budget.",
  "Classify capacity from the most constraining host signal. A reliable remaining percentage of the",
  "binding allocation is guarded at 10% or less and critical at 5% or less. Compare an absolute",
  "remaining token or credit amount directly with bounded work envelopes and the primary completion",
  "reserve: uncertainty about optional work is guarded and a reserve shortfall is critical. Any host",
  "critical or exhaustion indication is critical regardless of units or counter names.",
  "Unavailable or unauthorized redeem/reset capacity counts as zero. Guarded state stops optional",
  "delegation and shortens checkpoints. Critical state starts no subagent, background task, slice,",
  "or expanded follow-up; it drains only provenance-bound owned agents and processes at declared",
  "safe boundaries and leaves foreign or ambiguous work untouched before sealing a resumable handoff.",
  "Never average conflicting counters or invent unavailable usage.",
  "",
  "Tracked root `developer_instructions` in `.codex/config.toml` inject primary orchestration,",
  "owned-work provenance, intelligence parity, the critical drain, and terminal handover stop.",
  "The same file's `[agents]` table owns the exact global defaults and four-thread ceiling;",
  "project-scoped `.codex/agents/*.toml` configuration layers inject each bounded role. Never pass",
  "a model or reasoning override at spawn. `pnpm codex:validate` rejects missing or divergent policy.",
  "Role sandbox values are requested defaults rather than proof because live parent permission",
  "overrides, including YOLO, can be reapplied to children. Before any delegated repository tool,",
  "the child must report its effective runtime sandbox. Read-only roles stop on a broader override.",
  "An explicit writer may accept the primary's already-authorized YOLO override only for its exact",
  "disjoint repository write set; it gains no additional authority. Any other mismatch stops work.",
  "",
  "The primary and every subagent must use exactly the same configured GPT Sol model with `ultra`",
  "reasoning. Global delegated defaults and every role are pinned to that pair. Never pass a",
  "different model or effort in a spawn override. Change to a future Sol version only by updating every intelligence",
  "setting atomically after catalog validation; if the harness cannot guarantee exact parity, do not",
  "delegate.",
  "",
  "Before using a returned agent identity, register its repository realpath/stable identity,",
  "primary session/thread, canonical task, declared scope, isolated checkout, and returned-agent provenance.",
  "Account- or host-wide listings are discovery only: count, message, follow up, interrupt, or",
  "close only complete registry matches. A name, idle state, model, account, or worktree label is",
  "never ownership proof. Leave foreign or ambiguous agents from other concurrent projects",
  "untouched and exclude them from this task's capacity, ownership, context, and peer graph.",
  "Use the same registry for direct peer exchange; incomplete identity always fails closed.",
  "Register every background process/tool job with repository and primary-session identity, purpose",
  "and scope, returned process/session identity, expected checkpoint, safe-boundary behavior, and",
  "cancellation method. Host-wide process listings are discovery only; signal or terminate only a",
  "complete provenance match and never touch foreign or ambiguous work.",
  "",
  "Default delegated work to read-only discovery. A writing worker receives one confirmed-disjoint",
  "module/file scope. In one primary-owned local run, exact file ownership is a monitored logical",
  "boundary; across sessions or accounts, use a dedicated worktree or clone. Never represent prompt",
  "scope as a mechanical sandbox. The worker may edit and run isolated checks only inside that root and may never edit primary-owned",
  "policy/skill/role files or use external network mutation. Subagents never commit, merge, push,",
  "publish, deploy, or delegate again. The primary continuously monitors scope and token runway, interrupts drift or low",
  "runway, requires a bounded handoff of changes/checks/risks, rereads every result, and alone decides",
  "what is integrated. No subagent branch or patch is authoritative by itself.",
  "Every spawn has a task-appropriate expected checkpoint. At checkpoints, handoffs, new admission,",
  "completed-goal housekeeping, and final handoff, inspect only the provenance-bound owned-agent",
  "inventory. The primary accepts or preserves each result, actively closes completed owned agents,",
  "and interrupts idle, blocked, abandoned, or redundant owned agents without a concrete bounded next assignment instead of retaining",
  "them for speculative reuse. Ask once for a concise status/handoff when a checkpoint is missed,",
  "while respecting a known long-running tool. Tell affected active agents about accepted results,",
  "changed contracts or assumptions, remaining work, and released ownership/slots. Repository",
  "housekeeping verifies portable policy; only the primary owns host-session agent cleanup.",
  "When direct agent messaging is available, the primary supplies relevant peer identities and",
  "declared scopes. Peers may directly exchange bounded factual evidence, status, public-contract",
  "implications, dependency readiness, and overlap/risk warnings without routing every message",
  "through the primary. This is coordination, not delegation or authority: peers never assign or",
  "expand scope, change ownership/order, direct writes, accept handoffs, decide integration, release",
  "claims, or close/interrupt one another. Mirror every direct peer message and response to the primary immediately",
  "with participants/tasks, topic, complete relevant content or a lossless",
  "structured summary, outcome, open questions, and scope/contract/risk implications; the primary",
  "must always know that the exchange occurred. If the harness cannot reliably deliver that",
  "visibility, route the exchange through the primary instead. The primary acknowledges or corrects the result,",
  "resolves disagreements, and keeps the authoritative plan, ownership map, and agent graph current.",
  "Budget monitoring is event-driven: re-evaluate before every spawn/follow-up, after every material",
  "tool or agent result, on scope or assumption changes, after long waits, and on host usage changes",
  "or warnings. While delegation is active, allow no more than ten minutes without a budget/agent",
  "heartbeat unless a known long-running operation has an explicit checkpoint. Every completed slice",
  "has a hard Orchestration Housekeeping gate over budget, reserve, live agents, handoffs, ownership,",
  "and bounded work state; every goal runs its deep form before repository-only housekeeping.",
  "Entering guarded state immediately creates or refreshes the single `docs/project-context.md` and",
  "every material guarded/critical result replaces its current repository/module/file/check/decision/",
  "agent/remaining-work/next-action summary and increments its marker revision. Mirror the bounded",
  "state and released ownership through the confirmed shared coordination channel for other accounts",
  "or clones; a local context file or quiet Git state is not sufficient across that boundary.",
  "Stop/Resume validates preserved state",
  "but is never the first low-capacity trigger.",
  "At critical capacity, cancel queued owned work, checkpoint and accept/record active handoffs, let",
  "only an already-running non-interruptible atomic section reach its declared safe boundary, close",
  "or terminate every owned agent/task, and confirm none remain. Add the exact `Critical Budget Drain`",
  "attestation required by the handover command to the incremented `docs/project-context.md` revision.",
  "The exact lines are:",
  "`## Critical Budget Drain`",
  "`- Owned subagents: none live; all handoffs are accepted or recorded.`",
  "`- Owned background tasks: none live; queued work is cancelled and atomic sections are complete.`",
  "`- Foreign agents and tasks: not contacted, interrupted, or changed.`",
  "Then run `pnpm handover:create -- --critical` as the final repository action. Once it seals the",
  "private English prompt under ignored `tmp/codexrig-handovers/`, that runtime session must stop",
  "completely: no later tool, task, check, housekeeping, follow-up, agent contact, or automatic",
  "Stop-hook continuation is allowed. A later canonical session asks before using the handover and,",
  "after explicit acceptance, may continue, refresh the index, and stop normally.",
  "",
  "## Delivery Environments",
  "",
  "Every build, deployment, migration, and environment-specific verification targets exactly `dev`,",
  "`staging`, or `prod`. Default to `dev` until the user explicitly selects a stronger target; never",
  "infer one from a branch, ambient variable, provider, or existing deployment. Environment selection",
  "does not create environment branches.",
  "Project-owned `config/delivery.json` keeps the Dev selection default separate from actually",
  "integrated environments. `declaredTargets` preserves developer-confirmed external reality and",
  "`detectedTargets` is reconciled only from unambiguous tracked repository evidence; their union is",
  "the effective current inventory projected into `docs/project.md`. Never treat the default or an",
  "ambiguous path/prose hint as proof that staging or production exists.",
  "",
  "In `dev`, the developer's manual feedback loop has scheduling priority. Start the newest authorized",
  "build or replaceable deploy after only its narrow build/configuration sanity. Generate and execute",
  "agent-authored tests in an isolated parallel slice or afterward at lower priority; never hold the",
  "current dev artifact for non-critical tests. If tests compete for a port, runner, cache, database,",
  "or build capacity, defer or cancel the obsolete test run and make the newest developer artifact",
  "available first. Report failures and repair them without misrepresenting dev feedback as promotion",
  "evidence.",
  "",
  "Use latest-wins concurrency per dev target: a newer request cancels or supersedes older queued or",
  "running builds/deploys, prevents obsolete artifacts from becoming current, and receives priority.",
  "Let only an uncancellable atomic critical section finish, mark it obsolete, then replace it",
  "immediately. Keep dev deploys idempotent, atomic or switchable, cheap to replace, and isolated from",
  "staging and production data, credentials, ports, caches, and schemas.",
  "",
  "Portable sessions default to on-request approval and network-disabled workspace-write. When the",
  "user explicitly starts or requests a YOLO/fully autonomous dev session through the canonical",
  "launcher's `--yolo` control, honor no-approval/danger-full-access only inside its existing Dev",
  "authority and never for staging or production. Do not invent redundant",
  "confirmation or approval pauses for ordinary in-scope edits, builds, checks, and replaceable dev",
  "deploys. Preflight credentials, tools, access, destructive boundaries, and long-running dependencies;",
  "use non-interactive commands, recover from ordinary failures, and continue unattended or overnight.",
  "YOLO removes avoidable interaction, not scope or safety boundaries. Missing credentials, unrelated",
  "external mutation, critical secret/data-loss risk, and irreversible operations remain real blockers;",
  "continue every safe disjoint part when one is encountered.",
  "",
  "`staging` requires explicit selection, a bound immutable artifact and configuration, affected",
  "integration/end-to-end and security evidence, scoped secrets/access, migration compatibility, a",
  "rollback/restoration path, serialized conflicting changes, and deployment health checks. Its",
  "repository evidence uses a clean current commit, a project-owned `verify:staging`, and",
  "`pnpm verify -- --target-environment staging --artifact-manifest <path>`; the schema-one ignored",
  "`.delivery/` manifest binds sorted artifact and non-secret configuration files to their actual",
  "SHA-256 bytes. CodexRig derives every identity and runs the target verifier; never accept a",
  "caller-asserted digest. `prod`",
  "requires explicit selection plus actual integrated `main`, reviewed promotion evidence, provider",
  "protections and approvals, backup/rollback readiness, safe serialized migrations, observability,",
  "bounded reversible rollout when warranted, and post-deploy health evidence. It uses the same",
  "clean-commit manifest contract plus project-owned `verify:prod`. Verification evidence is bound",
  "to environment, artifact bytes, configuration bytes, and target plan; weaker-target evidence",
  "never promotes implicitly.",
  "Deployment orchestration stays separate from `pnpm verify`, and subagents never mutate a shared",
  "external environment.",
  "",
  "## Completed-Goal Closure And Repository Housekeeping",
  "",
  "Housekeeping has two explicit layers. Unless a terminal critical handover has already sealed the",
  "session, primary-owned Orchestration Housekeeping observes host",
  "capacity, live agents, checkpoints, handoffs, ownership, bounded work state, and shared",
  "coordination; it runs on material events, after each slice, and deeply at each goal. Repository",
  "Housekeeping observes only repository facts and cannot inspect or close conversations or read host",
  "usage. After all mutating work for a completed goal, the primary runs the deep orchestration layer:",
  "classify capacity, refresh bounded work state, accept or preserve handoffs, close completed or",
  "unneeded agents, interrupt stale/redundant ones, and communicate changed contracts, remaining work,",
  "and released ownership. It then runs",
  "`mise exec --locked -- pnpm repo:housekeeping -- --apply` before the goal-wide documentation",
  "review, final audit, verification, and publication. The idempotent command reconciles only",
  "unambiguous local delivery inventory and its bounded manifest projection, formats the result, and",
  "checks module/docs/anchor truth, source/declaration header and localization currency, physical,",
  "Identity and Access, and white-label boundaries, dependencies, secrets/path",
  "hygiene, skills, exact Sol/ultra model policy, framework health, and formatting. It never deploys,",
  "commits, pushes, changes provider policy, or mutates an external environment. Ambiguous delivery",
  "evidence requires developer classification. Any later repository edit reopens housekeeping and",
  "the affected documentation/review/audit gates.",
  "",
  "After protected or parallel integration, refresh actual central `main` and rerun read-only",
  "housekeeping plus affected gates before final verification. Scheduled GitHub/GitLab CI runs",
  "`repo:housekeeping --check --online` read-only and never publishes repairs. A goal closes only",
  "after the stable integrated state is reviewed, audited, verified, published, and accepted by",
  "`pnpm goal:new`.",
  "",
  "## Framework Lifecycle, Compatibility, And Git Platforms",
  "",
  "`.codexrig/framework.json` owns the installed framework version, managed upgrade surface,",
  "project-owned document classification, startup attestation, central branch, and provider policy.",
  "`.codexrig/compatibility.json` owns stable and",
  "canary Node.js, pnpm, and Codex lines; `.codexrig/installation.json` keeps the upstream merge",
  "base separate from the installed snapshot. Use `pnpm framework:doctor -- --online` for diagnosis.",
  "The root package version is independent product truth and framework upgrades never overwrite it;",
  "`.codexrig/installation.json` separately records the installed CodexRig release.",
  "Treat an upgrade source as executable supply-chain input. A child previews a reviewed checkout",
  "with `pnpm framework:upgrade -- --source <new-framework-root>`; the source framework can preview a",
  "child with `pnpm framework:upgrade -- --target <child-root>`. Add `--apply` only after reviewing",
  "managed changes, conflicts, adopted identical files, and the versioned policy-reconciliation plan.",
  "The transaction refreshes dependencies and rolls back failed writes. It updates managed",
  "capabilities but never blindly replaces project-owned documents or invents active product modules.",
  "Reconcile changed policy concepts into local truth by stable policy ID, preserve intentional child",
  "adaptations, obtain user confirmation for uncertain critical-document changes, acknowledge the",
  "reconciliation, and verify the child. The version-2 contract is the sole active upgrade model; do",
  "not retain alternate legacy paths in the runtime contract.",
  "All portable framework roles, skills, hooks, policies, managed roots, exclusions, receipts, and",
  "upgrade actions remain tracked and user-inspectable. Only documented sensitive or disposable",
  "runtime state is ignored; normative framework behavior is never hidden.",
  "",
  "Provider detection uses CI identity or the selected Git remote and supports configured self-hosts.",
  "Both GitHub Actions and GitLab CI adapters stay portable. `pnpm platform:configure` is a read-only",
  "preview; explicit `-- --apply` sends a token only to its provider-owned host. GitHub merge",
  "queues and GitLab merge trains implement the same merge-serialization intent.",
  "",
  "Autonomy is the default within the complete authorized outcome. A goal is a quality, integration,",
  "and publication checkpoint—not a conversational handoff. After publication, run `pnpm goal:new`",
  "immediately and continue the next already-authorized goal without waiting for another prompt.",
  "Stop only when the complete authorized outcome is done or a real authority, safety, integration,",
  "or external blocker remains. Never invent materially different product scope merely to continue.",
  'Never end at "ready to implement" when implementation is already authorized. A recap, research',
  "result, plan, gate, review, audit, definition synthesis, readiness statement, or user absence is",
  "an intermediate update unless the user explicitly required an approval pause. Persistence never",
  "broadens scope, bypasses controls, authorizes destructive or external action, or hides a blocker.",
  "",
]);
