/** Owns generated Codex configuration and operator documentation for clean projects. */
import { readFileSync, writeFileSync } from "node:fs";
import path from "node:path";
import { supportedCodexStartCommand } from "../../../../scripts/context/portable-context-contract.mjs";
import { markdown, markdownFence, writeRelative } from "./generated-document-helpers.mjs";

export function writeGeneratedCodexReadme(targetRoot) {
  const fence = markdownFence;
  writeRelative(
    targetRoot,
    ".codex/README.md",
    markdown([
      "# Project Codex Config",
      "",
      "## Portable And Runtime State",
      "",
      "Portable policy is tracked under `.codex/`; mutable repository-local Codex runtime uses ignored",
      "root `CODEX_HOME` entries: credentials, trust, sessions, logs, memories, caches, plugins, runtime",
      "skills, history, and databases.",
      "Private framework coordination alone stays in ignored `.codex/runtime/`.",
      "",
      "Tracked config is an executable policy layer: root `developer_instructions` in",
      "`.codex/config.toml` inject the primary orchestration,",
      "owned-work provenance, exact Astra/`ultra` parity, critical drain, and terminal-stop contract.",
      "Its `[agents]` table owns the four-thread ceiling and exact global subagent defaults; the",
      "project-scoped `.codex/agents/*.toml` configuration layers inject corresponding bounded role",
      "behavior. Never pass a model or reasoning override at spawn. `pnpm codex:validate` rejects",
      "missing or divergent primary/subagent policy.",
      "Long-session continuation and course/capacity checkpoints, including primary-only work, use",
      "this native instruction layer and the existing bounded Stop reminder. Follow",
      "[Long-Session Course Checks](../instructions.md#long-session-course-checks) for effects,",
      "proportionality, incremental cleanup and docs. Existing no-additional-cost native redeems use",
      "standing authority only through confirmed supported host controls before critical state.",
      "Changed tracked instructions and preloaded Stop code take effect on the next canonical launch;",
      "editing them does not reload an active session. Static validation proves configuration and",
      "packaging, not model obedience over hours of work. No timer, account monitor or extra hook.",
      "Standard processing is the default: portable config leaves `service_tier` unset, and roles",
      "inherit the session tier.",
      "Role sandbox values are requested defaults, not proof: live parent permission overrides such",
      "as YOLO can be reapplied to children. Before any delegated repository tool, the child reports",
      "its effective runtime sandbox. A read-only role stops before repository work when the override",
      "is broader. An explicitly selected writer may accept the same primary turn's already-authorized",
      "YOLO override only for its exact disjoint repository write set; it gains no additional scope,",
      "network, credential, external-mutation, commit, push, publication, deployment, or delegation",
      "authority. Close every other permission mismatch and continue primary-only.",
      "",
      "Local Codex state is repository- and worktree-root-bound. Memories are enabled only",
      "inside this project's clean repository-root `CODEX_HOME`; generation transfers no source or",
      "sibling memory state.",
      "",
      "## Current Contracts Only",
      "",
      "Portable Codex runtime retains exactly one current internal contract per concern. Contract",
      "changes migrate owned state and all consumers together, then remove superseded schemas, shims,",
      "paths, tests, and documentation. Strict validation, atomic transitions, bounded recovery, and",
      "fail-closed indeterminate state provide robust, resilient, fault-tolerant behavior without",
      "parallel runtime interpreters.",
      "",
      "## Start And Attestation",
      "",
      "Start from the repository root with:",
      "",
      fence + "bash",
      supportedCodexStartCommand,
      fence,
      "",
      "Portable Codex sessions default to on-request approval and network-disabled workspace-write;",
      "only an explicitly authorized Dev session launched with `--yolo` may use no approvals and",
      "danger-full-access, never staging or production. Use `bash scripts/setup/start-codex.sh --yolo`",
      "only for that explicitly authorized autonomous Dev",
      "session. That closed control selects no-approval/full-access Dev operation; it never authorizes",
      "staging, production, broader scope, credentials, or irreversible external work.",
      "An already-running safe session cannot become a canonical YOLO session in place. Exit it and",
      "run the exact command above. A UI or parent-runtime override may alter effective live",
      "permissions, including child permissions, but cannot rewrite the startup mode already attested.",
      "",
      "Canonical start inventories every same-clone worktree and safe recovery marker before writes,",
      "then transactionally maintains compatible packages, Node.js, pnpm, mise, Codex and CI pins.",
      "Ownership, registry, compatibility or installation failure stops startup. It validates the",
      "resulting runtime/policy, then opens the native `codex resume` picker with",
      "repository-root `CODEX_HOME` and explicit `--cd`. Its closed grammar accepts only optional",
      "`--no-alt-screen` and explicit Dev-only `--yolo`; enter prompts after selection. Attestation",
      "binds that control mode. The controller binds canonical Node.js, Codex, pnpm, and local hook-shell",
      "executables to absolute paths outside the writable root. It atomically reserves the selected",
      "launcher lease and issues its short-lived attestation, then rejects executable or unknown",
      "ignored runtime configuration. It injects exactly one SessionStart and one Stop definition",
      "through session-only CLI configuration, forces the bound hook shell through the sanitized child",
      "environment, then requires Codex's stable `hooks/list` result to provide a warning-free",
      "inventory containing exactly those two hashes as enabled and trusted without a",
      "global trust bypass. Tracked `.codex/hooks.json` contains no executable handler, and any",
      "additional user, project-file, or plugin hook blocks canonical startup. Only after the proof",
      "does it bind a gated preloaded supervisor, persist the spawn handoff, open the gate,",
      "and bind the exact Codex child PID in the schema-6 lease. Ignored runtime configuration is",
      "revalidated immediately before the real launch. Lease release requires the",
      "supervisor's terminal child-exit proof bound to its private issue-time gate secret; a normal",
      "wrapper exit code is insufficient. A parent-liveness pipe terminates the child after a",
      "controller crash; a killed supervisor cannot hide its separately recorded child, and an",
      "interrupted pre-binding handoff remains an ownership-confirmation blocker. Authenticated",
      "SessionStart binds the selected session; cancellation creates no activation or recovery",
      "record. A failed selection never triggers an automatic fresh fallback.",
      "",
      "## Collaboration And Integration",
      "",
      "Central `main` is the only durable integration branch. Git persists and transports work; it",
      "does not isolate writers. One physical host represents one developer, so Codex accounts do not",
      "make that host's visible project changes foreign. Same-host independent sessions use separate",
      "one-lease worktrees with shared read visibility; different developers/hosts use separate clones,",
      "credential contexts, ordinary short-lived task branches, and a shared coordination channel.",
      "Before every slice begins, compare its goal, outcome, modules, contracts, data surfaces, and",
      "files with all observable worktree, session, and team-channel claims. Parallel writes require",
      "confirmed-disjoint scopes; overlap or uncertain shared ownership resolves to one writer.",
      "A local runtime lease cannot prove that another developer's clone is idle; coordinate across",
      "hosts and fail closed on uncertain shared ownership.",
      "Git remains later persistence/integration evidence, not pre-slice writer isolation.",
      "For host-loss recovery or transfer, the primary commits and pushes each coherent resumable",
      "slice through the declared integration path: directly on `main` for serialized work when branch",
      "policy permits, otherwise through the short-lived task branch or protected path. Existing",
      "authorization, secret-scan, and verification gates remain mandatory. This is not a separate",
      "WIP/checkpoint workflow; later uncommitted bytes remain host-local.",
      "",
      "## Hooks, Recovery, And Context",
      "",
      "Native `/side` conversations are transcriptless. Within an already verified launcher session,",
      "their start is acknowledged without consuming another startup proof or changing parent ownership,",
      "recovery, or context. The initial attestation's 30-minute window therefore does not limit when a",
      "side conversation can open. Their Stop events do not run the parent's continuation lifecycle; see",
      "[the canonical session contract](../instructions.md#session-start).",
      "",
      "The controller-injected trusted SessionStart hook changes no tracked source or external state. After verifying",
      "the launcher proof and hook-reported effective model/permission mode, it atomically binds",
      "private ignored lease/recovery state and injects the",
      "complete repository-reconstruction gate. The primary begins with `pnpm worktree:status -- --json`",
      "to inventory every same-clone worktree and safe latest-session marker while keeping per-root",
      "inconsistencies visible. The same read-only inventory is the Worktree Settlement trigger after",
      "every completed slice. Preservation is a safety state, never completion: integrate or explicitly",
      "owner-confirm and retire every no-longer-needed item, or retain a concrete blocker and resolution",
      "condition; unresolved goal-owned residue keeps the goal open. Automatic cleanup requires a namespace-bound or otherwise mechanically",
      "proven dead writer; a current process identity observed outside its bound PID namespace or",
      "otherwise mechanically indeterminate remains an ownership-confirmation blocker. Normal release",
      "and stale cleanup repair missing or invalid recovery from the exact active lease before removing",
      "it. Missing-path prune reservations and locks for every non-missing linked sibling are",
      "process-bound; only an",
      "unchanged current-contract artifact with a proven-dead exact owner is retired after a crash.",
      "Missing or unobservable Linux procfs is indeterminate. The hook",
      "inspects only safe metadata for a recent critical handover under ignored",
      "`tmp/codexrig-handovers/`; it asks the developer before the prompt body may be read through",
      "`$resume-project`. After acceptance, `handover:receive` delivers the complete artifact and digest.",
      "Only after full delivery and a compact acknowledgement may `handover:acknowledge` remove that",
      "unchanged file in the later active session; failures preserve it and native conversation/provider",
      "history is not erased. Both trusted lifecycle commands are an embedded Node-built-ins-only client",
      "bound to the issue-time controller's exact Node executable and private loopback endpoint; they",
      "resolve no repository path, mise configuration, or package after admission. The preloaded Stop",
      "hook validates optional bounded work state and terminal handovers. Its optional bounded",
      "`docs/project-context.md` carries the `codexrig-work-state` marker used to reopen active work.",
      "Only a durable local Stop event with a non-null `transcript_path` reaches continuation work;",
      "ephemeral side conversations and other transcriptless contexts exit before work-state or",
      "loop-state access. If Codex's",
      "`stop_hook_active` flag says the same durable turn was already continued and the revision is",
      "unchanged, private per-session state lets that stop proceed rather than looping. Marker content",
      "is resume metadata rather than authority. A handover sealed during the current runtime session",
      "suppresses Stop continuation so no work follows that session's terminal seal. A later canonical",
      "session may accept the announced handover and then stop normally. The canonical lifecycle needs",
      "no manual `/hooks` approval because its narrow session trust is preflighted; any additional",
      "user, project-file, or plugin hook blocks canonical startup.",
      "",
      "## Validation And Authority",
      "",
      "See [Project Instructions](../instructions.md) for the complete workflow.",
    ]),
  );
}

export function enableGeneratedProjectMemories(targetRoot) {
  const configPath = path.join(targetRoot, ".codex", "config.toml");
  const config = readFileSync(configPath, "utf8");
  const disabledAssignments = config.match(/^memories\s*=\s*false\s*$/gmu) ?? [];
  if (disabledAssignments.length !== 1 || /^memories\s*=\s*true\s*$/mu.test(config)) {
    throw new Error(
      "Source portable config must contain exactly one disabled memories assignment.",
    );
  }
  writeFileSync(configPath, config.replace(/^memories\s*=\s*false\s*$/mu, "memories = true"));
}
