interface:
  display_name: "System Coherence"
  short_description: "Review code quality and system integration"
  default_prompt:
    "Use $system-coherence to review this change and its real consumers. Stay read-only unless
    repairs are already authorized within the declared implementation scope."
